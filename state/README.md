# State Directory

This directory stores structured monitoring output for the central bank speech tracker.

## Files

- `cb_speeches_status.json`
- `cb_speeches_events.jsonl`

## Notes

- `pipeline_logs` in SQLite remains the source of stage timing history.
- These files are the dashboard-facing snapshot and event stream.
